# План миграции функциональности из команды `new` в `projects`

## 📋 Обзор
Объединить функционал создания проектов с просмотром списка проектов в единую команду `projects` и полностью удалить команду `new`.

## 🎯 Цели
- Упростить интерфейс пользователя (одна команда вместо двух)
- Логически сгруппировать связанные операции с проектами
- Уменьшить количество кода и контекстов
- Сохранить всю текущую функциональность

## 📊 Анализ текущей реализации

### Команда `new` (`app/commands/new_command.rb`)
**Функциональность:**
- Создание новых проектов
- Два режима работы:
  1. Прямое создание: `/new project-slug`
  2. Многошаговый: `/new` → запрос slug → ввод названия

**Ключевые особенности:**
- Использует контекст `NEW_PROJECT_SLUG_INPUT` для многошагового создания
- Автоматически назначает пользователю роль `owner` для нового проекта
- Поддерживает валидацию slug (не пустой, уникальный)
- Есть полноценные тесты (`new_command_spec.rb`)

**Интерфейс:**
- `/new [slug]` - создает проект с указанным slug
- `/new` - запускает интерактивное создание проекта
- Возвращает "Создан проект `project.slug`" при успехе

### Команда `projects` (`app/commands/projects_command.rb`)
**Функциональность:**
- Отображение списка доступных проектов
- Показывает активные (неархивные) проекты
- Поддерживает разные роли пользователя (owner, member, viewer)
- Форматирует информацию о клиентах проектов

**Ключевые особенности:**
- Использует `current_user.available_projects.alive` для фильтрации
- Показывает проекты с клиентами в формате "Project (Client)"
- Есть полные тесты (`projects_command_spec.rb`)

**Интерфейс:**
- `/projects` - отображает список проектов в формате:
  ```
  Доступные проекты:
  • Project Name
  • Project Name (Client Name)
  ```

## 📝 План работ

### Phase 1: Расширение команды `projects`
1. **Изменить `app/commands/projects_command.rb`**:
   - Добавить обработку аргументов (`create`, `create [slug]`)
   - Реализовать метод `create_project(slug)`
   - Реализовать метод `create_project_interactive`
   - Обновить метод `call` для маршрутизации

2. **Реализовать валидацию и бизнес-логику**:
   - Проверка slug на пустоту и уникальность
   - Автоматическое назначение роли `owner`
   - Обработка ошибок с человеко-читаемыми сообщениями

3. **Обновить интерфейс пользователя**:
   - `/projects` - показать список проектов
   - `/projects create` - интерактивное создание проекта
   - `/projects create project-slug` - прямое создание

### Phase 2: Обновление тестов
1. **Расширить `spec/controllers/telegram/webhook/projects_command_spec.rb`**:
   - Добавить тесты на создание проектов
   - Добавить тесты на валидацию slug
   - Добавить тесты на роль `owner`
   - Проверить все сценарии использования

2. **Удалить устаревшие тесты**:
   - Удалить `spec/controllers/telegram/webhook/new_command_spec.rb`

### Phase 3: Очистка кода
1. **Удалить команду `new`**:
   - Удалить файл `app/commands/new_command.rb`

2. **Удалить контекст `NEW_PROJECT_SLUG_INPUT`**:
   - Удалить константу из `app/commands/base_command.rb`
   - Удалить метод `new_project_slug_input`
   - Удалить обработку контекста из `app/controllers/telegram/webhook_controller.rb`

3. **Обновить документацию**:
   - Обновить README.md с новым синтаксисом команд
   - Обновить справку по командам бота

## 🔍 Ключевые детали реализации

### Новые методы в ProjectsCommand
```ruby
def create_project(slug)
  # Валидация slug
  # Создание проекта
  # Назначение роли owner
  # Отправка подтверждения
end

def create_project_interactive
  # Запрос slug у пользователя
  # Установка контекста для ввода
end

def handle_project_creation_input
  # Обработка ввода пользователя
  # Создание проекта
  # Очистка контекста
end
```

### Обновленный интерфейс ProjectsCommand
```ruby
def call(*args)
  case args.first
  when 'create'
    handle_create_command(args[1])
  when nil
    show_projects
  else
    # Неизвестная команда
  end
end
```

## 🔍 Ключевые детали
- Сохранить все правила валидации slug из текущей реализации
- Автоматически назначать создателю роль `owner`
- Поддерживать как интерактивное, так и прямое создание проектов
- Полностью удалить зависимость от контекста `NEW_PROJECT_SLUG_INPUT`

## ⚠️ Риски и митигация

### Риски
- Обратная совместимость для пользователей, привыкших к команде `/new`
- Необходимо тщательно протестировать все сценарии создания проектов
- Убедиться что все тесты покрывают новую функциональность

### Митигация
- Добавить информационное сообщение о迁移е команды
- Создать comprehensive тесты для всех сценариев
- Постепенное развертывание с мониторингом

## ✅ Критерии завершения
- [ ] Команда `/projects` показывает список проектов
- [ ] Команда `/projects create` запускает интерактивное создание
- [ ] Команда `/projects create slug` создает проект напрямую
- [ ] Все тесты из `new_command_spec.rb` перенесены в `projects_command_spec.rb`
- [ ] Файл `new_command.rb` удален
- [ ] Контекст `NEW_PROJECT_SLUG_INPUT` удален
- [ ] Документация обновлена
- [ ] Все тесты проходят успешно

## 📋 Файлы для изменения

### Модификация
- `app/commands/projects_command.rb` - расширение функционала
- `spec/controllers/telegram/webhook/projects_command_spec.rb` - новые тесты
- `app/commands/base_command.rb` - удаление константы
- `app/controllers/telegram/webhook_controller.rb` - удаление обработки контекста

### Удаление
- `app/commands/new_command.rb`
- `spec/controllers/telegram/webhook/new_command_spec.rb`

### Документация
- `README.md`
- Справка по командам бота