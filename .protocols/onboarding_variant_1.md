# Спецификация: Onboarding новичка (Вариант 1)

**Статус**: выполнено
**Версия**: 1.0
**Дата**: 2025-11-16

---

## 1. Обзор

Быстрый onboarding для новых пользователей, которые пришли в бот впервые без проектов. Цель: помочь пользователю за 2-3 шага понять, что такое Masha и как отмечать время.

**Ключевые принципы**:
- ✅ Минимум текста, максимум действий
- ✅ Inline кнопки вместо сложных команд
- ✅ Демонстрация на реальном примере
- ✅ Ссылки на справку вместо длинных объяснений
- ✅ Пользователь создает проект и отмечает время на первом же сеансе

**Метрика успеха**: пользователь создал проект и добавил в него одну запись времени.

---

## 2. User Journey

```
Новый пользователь (нет проектов)
           ↓
    /start (приветствие)
           ↓
  Сообщение с inline кнопками:
  [📖 Как это работает?] [➕ Создать проект] [⏱️ Добавить время]
           ↓
   Пользователь нажимает [➕ Создать проект]
           ↓
  Бот: "Укажите slug проекта (например: my-project)"
           ↓
 Пользователь: "my-first-project"
           ↓
  Бот: "✅ Проект создан! Теперь добавьте время:"
  [🕐 Добавить время] [📖 Справка]
           ↓
 Пользователь: "2 my-first-project Первая задача"
           ↓
  Бот: "✅ Добавлено 2 часа в проект 'my-first-project'"
           ↓
      ONBOARDING COMPLETE
```

---

## 3. Telegram Bot Сценарии

### 3.1 `/start` - Приветствие новичка

**Условие**: пользователь впервые в боте И у него нет проектов

**Сообщение бота**:
```
Добро пожаловать в Masha! ⏱️

Это бот для отслеживания времени, которое вы тратите на разные задачи.

Начните с создания проекта и добавления записи времени:
```

**Inline кнопки**:
```
[📖 Как это работает?]  [➕ Создать проект]
[⏱️  Добавить время]
```

**Поведение кнопок**:
- **"📖 Как это работает?"** → Ссылка на веб-справку `/help/guide`
- **"➕ Создать проект"** → Запускает команду `/projects create` (интерактивный режим)
- **"⏱️  Добавить время"** → Отправляет inline кнопки для выбора проекта ИЛИ предлагает создать если проектов нет

---

### 3.2 Создание проекта `/projects create` (интерактивный)

**Текущее поведение**: ✅ Уже есть

Бот просит `slug` → Пользователь вводит → Проект создан

**Добавление после создания**:
После успешного создания проекта показать:
```
✅ Проект 'my-first-project' создан!

Теперь добавьте время:
[⏱️  Добавить время в этот проект]
```

Эта кнопка должна запустить `/add my-first-project` (предзаполненный проект).

---

### 3.3 Добавление времени без проектов (ERROR HANDLING)

**Условие**: пользователь пытается добавить время, а проектов нет

**Сценарий 1: Через команду `/add`**
```
Бот вместо ошибки:

У вас нет проектов 😔

Создайте первый проект:
[➕ Создать проект]

Или используйте быструю команду:
/projects create my-project
```

**Сценарий 2: Через сообщение `"2 my-project описание"`**
```
Бот:
Проект 'my-project' не найден 🤔

Создайте его или используйте свой:
[➕ Создать проект] [📖 Справка]
```

---

### 3.4 Первое добавление времени (с подсказкой)

**После создания проекта пользователь нажимает [⏱️  Добавить время]**

Запускается `/add my-first-project`

**Бот показывает**:
```
Отлично! Добавляем время в 'my-first-project'

Отправьте сообщение в формате:
2 мое описание

где:
  2 — часы (например: 1.5, 3, 0.5)
  мое описание — что вы делали

Пример: 2 Работа над задачей

[📖 Подробнее]
```

**После успешного добавления**:
```
✅ Добавлено 2 часа в 'my-first-project'

Вы на правильном пути! 🎉

[📊 Просмотреть отчет] [➕ Еще время] [📖 Справка]
```

---

## 4. Веб-страницы справки

Необходимо создать отдельные веб-страницы (маршруты) для справки:

### 4.1 Структура маршрутов

```
/help/                    — Главная страница справки
/help/guide               — Как это работает (полный гайд)
/help/quick-start         — Быстрый старт (для новичков)
/help/commands            — Справка по всем командам
/help/time-format         — Форматы добавления времени
/help/projects            — Управление проектами
/help/faq                 — Часто задаваемые вопросы
```

### 4.2 Страница `/help/quick-start` (для новичков)

**Заголовок**: Быстрый старт в Masha

**Содержание**:
1. **Что такое Masha?** (1 параграф)
2. **Три шага для начала**:
   - Создать проект
   - Добавить время
   - Посмотреть отчет
3. **Примеры команд** (копируемые блоки)
4. **Частые ошибки** (2-3 примера)
5. **Ссылки на остальную справку**

---

### 4.3 Страница `/help/guide` (полный гайд)

**Заголовок**: Полное руководство пользователя Masha

**Разделы**:
1. **Основные понятия**
   - Проект
   - Запись времени
   - Роли в проектах
2. **Как начать**
   - Регистрация
   - Создание проекта
3. **Отмечать время**
   - Форматы добавления
   - Примеры
4. **Отчеты**
   - Команда `/report`
   - Фильтры по времени и проектам
5. **Управление проектами**
   - Команда `/projects`
   - Командаивание пользователями (если есть)

---

## 5. Технические детали реализации

### 5.1 Определение "новичка"

**Условие**: пользователь в `users` таблице И у него `0` проектов (условие в базе)

```ruby
user.memberships.count == 0
```

### 5.2 Модификация команды `/start`

**Файл**: `app/commands/start_command.rb`

```ruby
def execute!
  if user.memberships.empty?
    respond_with_newbie_greeting
  else
    respond_with_regular_greeting
  end
end

private

def respond_with_newbie_greeting
  text = t('commands.start.welcome_new_user')

  respond_with :message,
               text: text,
               reply_markup: {
                 inline_keyboard: [
                   [
                     { text: '📖 Как это работает?', url: help_guide_url },
                     { text: '➕ Создать проект', callback_data: 'onboarding:create_project' }
                   ],
                   [
                     { text: '⏱️  Добавить время', callback_data: 'onboarding:add_time' }
                   ]
                 ]
               }
end
```

### 5.3 Callback обработчики для onboarding

**Файл**: `app/commands/onboarding_command.rb` (новый файл)

Обрабатывает callback'и с префиксом `onboarding:*`:
- `onboarding:create_project` → запускает `/projects create`
- `onboarding:add_time` → показывает список проектов ИЛИ предлагает создать

### 5.4 Модификация `/add` команды

При запуске `/add` без проектов показать:
```ruby
def execute!(project_slug = nil)
  if user.memberships.empty?
    respond_with_no_projects_error
  else
    # обычная логика
  end
end

private

def respond_with_no_projects_error
  text = t('commands.add.no_projects')

  respond_with :message,
               text: text,
               reply_markup: {
                 inline_keyboard: [
                   [{ text: '➕ Создать проект', callback_data: 'onboarding:create_project' }],
                   [{ text: '📖 Справка', url: help_time_format_url }]
                 ]
               }
end
```

### 5.5 Модификация `/projects create` успешного ответа

После успешного создания проекта добавить кнопку:
```ruby
respond_with :message,
             text: "✅ Проект '#{project.slug}' создан!\n\nТеперь добавьте время:",
             reply_markup: {
               inline_keyboard: [
                 [{ text: '⏱️  Добавить время', callback_data: "onboarding:add_time:#{project.slug}" }],
                 [{ text: '📖 Справка', url: help_time_format_url }]
               ]
             }
end
```

---

## 6. i18n Тексты

**Файл**: `config/locales/ru.yml`

```yaml
ru:
  commands:
    start:
      welcome_new_user: |
        Добро пожаловать в Masha! ⏱️

        Это бот для отслеживания времени, которое вы тратите на разные задачи.

        Начните с создания проекта и добавления записи времени:

    add:
      no_projects: |
        У вас нет проектов 😔

        Создайте первый проект:

    projects:
      create_success: |
        ✅ Проект '%{slug}' создан!

        Теперь добавьте время:
```

---

## 7. Web Pages (Rails Controllers)

### 7.1 Новый маршрут

```ruby
# config/routes.rb
get '/help', to: 'help#index'
get '/help/guide', to: 'help#guide'
get '/help/quick-start', to: 'help#quick_start'
get '/help/commands', to: 'help#commands'
get '/help/time-format', to: 'help#time_format'
get '/help/projects', to: 'help#projects'
get '/help/faq', to: 'help#faq'
```

### 7.2 Контроллер

```ruby
# app/controllers/help_controller.rb
class HelpController < ApplicationController
  skip_authentication # открытые страницы

  def index
    # главная страница справки
  end

  def guide
    # полное руководство
  end

  def quick_start
    # быстрый старт для новичков
  end

  # остальные методы...
end
```

---

## 8. Примеры использования

### Сценарий 1: Полный путь новичка

```
1. Пользователь: /start
2. Бот: [Приветствие] [📖] [➕ Создать] [⏱️]
3. Пользователь: нажимает [➕ Создать]
4. Бот: "Укажите slug:"
5. Пользователь: "my-work"
6. Бот: "✅ Проект создан! [⏱️  Добавить время] [📖]"
7. Пользователь: нажимает [⏱️]
8. Бот: "Отправьте время в формате: 2 описание"
9. Пользователь: "3 Первая задача"
10. Бот: "✅ Добавлено 3 часа! [📊] [➕] [📖]"
```

### Сценарий 2: Если пользователь попробует `/add` без проектов

```
1. Пользователь: /add
2. Бот: "У вас нет проектов [➕ Создать] [📖]"
3. Пользователь: нажимает [➕]
4. Бот: просит slug
5. Пользователь: вводит slug
6. Бот: проект создан, показывает [⏱️]
7. Пользователь: продолжает как в Сценарии 1
```

---

## 9. Тестирование (критерии приемки)

- [ ] Новый пользователь видит приветствие с кнопками при `/start`
- [ ] Кнопка "➕ Создать проект" запускает интерактивное создание
- [ ] После создания проекта показывается кнопка "⏱️  Добавить время"
- [ ] При нажатии на кнопку можно сразу добавить время
- [ ] После добавления времени пользователь видит подтверждение и ссылку на отчет
- [ ] Если пользователь вызовет `/add` без проектов, он видит ошибку с кнопкой создания
- [ ] Веб-страницы справки открываются по ссылкам из бота
- [ ] Все тексты корректно переводятся на русский

---

## 10. Приоритет реализации

1. ✅ Модификация `/start` (приветствие новичка)
2. ✅ Обработчик `onboarding:create_project` callback
3. ✅ Модификация ответа `/projects create`
4. ✅ Обработчик ошибки в `/add` (нет проектов)
5. ✅ Веб-страницы справки (`/help/*`)
6. ✅ i18n тексты
7. ✅ Тестирование

---

## Приложение: Макеты сообщений

### Приветствие новичка (markdown)
```
Добро пожаловать в Masha! ⏱️

Это бот для отслеживания времени, которое вы тратите на разные задачи.

Начните с создания проекта и добавления записи времени:
```

### После создания проекта
```
✅ Проект 'my-first-project' создан!

Теперь добавьте время:
```

### После добавления времени
```
✅ Добавлено 2 часа в 'my-first-project'

Вы на правильном пути! 🎉
```

---

**Статус**: ✅ Спецификация готова к реализации
