# Спецификация интерактивной команды /clients

## Анализ текущей реализации

### Команда /projects (интерактивный образец)
- **Интерфейс**: Инлайн-кнопки, навигация по меню, многошаговые диалоги
- **Архитектура**: 
  - Callback query методы (`projects_select:*`, `projects_rename:*`, etc.)
  - Context methods для обработки текстового ввода (`awaiting_project_name`, etc.)
  - Session management для хранения состояния
  - Подтверждение критических действий (удаление через ввод названия)
- **Функционал**: Создание, переименование (title/slug/both), управление клиентом, удаление с подтверждением

### Команда /clients (текущая текстовая версия)
- **Интерфейс**: Только текстовые команды с аргументами
- **Архитектура**:
  - `call(subcommand, *args)` - разбор текстовых команд
  - Context methods для многошагового ввода (`add_client_name` → `add_client_key`)
  - Нет callback query методов и инлайн-кнопок
- **Функционал**: Добавление, просмотр, редактирование имени, удаление (только если нет проектов)

## Спецификация новой интерактивной команды /clients

### 1. Общая архитектура

```ruby
class ClientsCommand < BaseCommand
  provides_context_methods(
    :awaiting_client_name,        # Ввод имени нового клиента
    :awaiting_client_key,         # Ввод ключа нового клиента
    :awaiting_client_rename,      # Ввод нового имени при редактировании
    :awaiting_client_delete_confirm # Подтверждение удаления
  )
  
  # Callback query methods
  def clients_create_callback_query(data = nil); end
  def clients_select_callback_query(data); end
  def clients_list_callback_query(data = nil); end
  def clients_rename_callback_query(data); end
  def clients_projects_callback_query(data); end
  def clients_delete_callback_query(data); end
  def clients_delete_confirm_callback_query(data); end
  
  # Context methods
  def awaiting_client_name(*name_parts); end
  def awaiting_client_key(*key_parts); end
  def awaiting_client_rename(*name_parts); end
  def awaiting_client_delete_confirm(*confirm_parts); end
end
```

### 2. Флоу взаимодействия

#### Главный экран (`/clients`)
```
┌─────────────────────────────┐
│ Ваши клиенты:               │
│                             │
│ [Добавить клиента]          │
│                             │
│ [client1] [client2] [client3]│
│ [client4] [client5] [client6]│
│                             │
└─────────────────────────────┘
```
- Список кнопок клиентов (по 3 в ряд, `key.truncate(15)`)
- Кнопка "Добавить клиента" на всю ширину
- Кнопки клиентов имеют `callback_data: "clients_select:{key}"`

#### Меню клиента (`clients_select:{key}`)
```
┌─────────────────────────────┐
│ Клиент: ACME Corp (acme)    │
│ Проектов: 3                 │
│                             │
│ [Переименовать]             │
│ [Показать проекты]          │
│ [Удалить] ← если проектов=0 │
│                             │
│ [Назад]                     │
└─────────────────────────────┘
```
- Кнопка "Переименовать" → `clients_rename:{key}`
- Кнопка "Показать проекты" → `clients_projects:{key}`
- Кнопка "Удалить" → `clients_delete:{key}` (показывается только если `client.projects.empty?`)
- Кнопка "Назад" → `clients_list:`

#### Создание клиента
1. Нажатие "Добавить клиента" → `clients_create_callback_query`
2. `save_context(:awaiting_client_name)`
3. Сообщение: "Введите имя нового клиента (или 'cancel' для отмены)"
4. Ввод имени → `awaiting_client_name`
   - Валидация: не пустое, ≤255 символов
   - Сохранение в `session[:new_client_name]`
5. Запрос ключа: "Введите ключ (a-z0-9_- , 2-50 символов) или 'cancel'"
   - Генерация предложенного ключа: `name.parameterize`
   - Показ кнопки `[Использовать предложенный: {suggested_key}]`
6. Ввод ключа → `awaiting_client_key`
   - Валидация: формат, уникальность для пользователя
   - Создание клиента: `Client.create!(user: current_user, name: session[:new_client_name], key: key)`
   - Очистка session
   - Сообщение: "Клиент '{name}' создан с ключом '{key}'"
   - Возврат к списку: `show_clients_list`

#### Переименование клиента (`clients_rename:{key}`)
1. `session[:edit_client_key] = key`
2. `save_context(:awaiting_client_rename)`
3. Показ текущего имени: "Введите новое имя для клиента {current_name}"
4. Ввод → `awaiting_client_rename`
5. Обновление: `client.update(name: new_name)`
6. Сообщение об успехе, возврат в меню клиента

#### Просмотр проектов клиента (`clients_projects:{key}`)
```
┌─────────────────────────────┐
│ Проекты клиента ACME (acme):│
│                             │
• Проект Alpha (alpha)        │
• Проект Beta (beta)          │
• Проект Gamma (gamma)        │
│                             │
│ [Назад]                     │
└─────────────────────────────┘
```
- Список проектов в тексте (`• {project.name} ({project.slug})`)
- Кнопка "Назад" → `clients_select:{key}`

#### Удаление клиента (`clients_delete:{key}`)
Только если `client.projects.empty?`
1. Подтверждение: "Удалить клиента {name} ({key})? Это действие нельзя отменить"
2. Кнопки: `[Да, удалить]` → `clients_delete_confirm:{key}` и `[Назад]`
3. `clients_delete_confirm:{key}` → `client.destroy`
4. Сообщение: "Клиент удален", возврат к списку

### 3. Callback data формат

| Callback | Описание | Data format |
|----------|----------|-------------|
| `clients_create:` | Начать создание клиента | Пусто |
| `clients_select:{key}` | Выбрать клиента | `clients_select:acme_corp` |
| `clients_list:` | Показать список клиентов | Пусто |
| `clients_rename:{key}` | Начать переименование | `clients_rename:acme_corp` |
| `clients_projects:{key}` | Показать проекты | `clients_projects:acme_corp` |
| `clients_delete:{key}` | Подтверждение удаления | `clients_delete:acme_corp` |
| `clients_delete_confirm:{key}` | Выполнить удаление | `clients_delete_confirm:acme_corp` |
| `clients_use_suggested_key:{key}` | Использовать предложенный ключ | `clients_use_suggested_key:acme_corp` |

### 4. Context methods

#### `awaiting_client_name(*name_parts)`
- Валидация: не пустое, ≤255 символов
- При cancel: выход в список клиентов
- Сохранение: `session[:new_client_name]`
- Следующий шаг: `save_context(:awaiting_client_key)`

#### `awaiting_client_key(*key_parts)`
- Валидация: формат `[a-z0-9_-]{2,50}`, уникальность
- Показывать кнопку с предложенным ключом
- При cancel: очистка session, выход в список
- Создание клиента, очистка session
- Возврат к списку

#### `awaiting_client_rename(*name_parts)`
- `key = session[:edit_client_key]`
- Валидация: не пустое, ≤255 символов
- Обновление: `client.update(name: new_name)`
- Очистка session, возврат в меню клиента

#### `awaiting_client_delete_confirm(*parts)`
- Проверка: подтверждение текстом (как в projects)
- Удаление: `client.destroy` if confirm
- Возврат к списку

### 5. Обратная совместимость

#### Текстовые команды (сохранить работоспособность):
- `/clients` → показывает новый интерактивный список
- `/clients add` → запускает `clients_create_callback_query` (вместо текущего `handle_add_client`)
- `/clients show {key}` → показывает информацию текстом (без изменений)
- `/clients edit {key}` → запускает `clients_rename_callback_query`
- `/clients delete {key}` → запускает `clients_delete_callback_query`
- `/clients delete {key} confirm` → немедленное удаление (без подтверждения)
- `/clients projects {key}` → показывает проекты текстом
- `/clients help` → показывает помощь текстом

#### Легаси формат:
```ruby
def handle_legacy_format(args)
  # Обработка старых команд через call(*args)
  # Вызов соответствующих callback/query методов
end
```

### 6. Валидация и ограничения

| Поле | Валидация | Ошибка |
|------|-----------|--------|
| `name` | presence, length ≤ 255 | "Имя не может быть пустым/слишком длинным" |
| `key` | format: `/\A[a-z0-9_-]{2,50}\z/`, уникальность | "Неверный формат/ключ занят" |
| Удаление | Только если `projects.empty?` | "Нельзя удалить: есть проекты" |

### 7. Структура сессии

```ruby
session[:new_client_name] # Временное имя при создании
session[:suggested_key]    # Предложенный ключ
session[:edit_client_key]  # Ключ клиента при редактировании
```

### 8. Метрики и логирование

- Все действия логировать в `Rails.logger.info`
- Использовать `Bugsnag.notify` при ошибках callback без data
- Сообщения об успехе/ошибке должны быть понятными

### 9. Тестирование (RSpec)

**Тестовые сценарии:**
1. Показ списка клиентов с кнопками
2. Создание клиента через многошаговый диалог
3. Переименование клиента
4. Просмотр проектов клиента
5. Удаление клиента (успех/отказ из-за проектов)
6. Обратная совместимость текстовых команд
7. Валидация ошибок (пустое имя, занятый ключ)

### 10. Интеграция

**Файлы для изменения:**
1. `app/commands/clients_command.rb` - полная переработка
2. `config/locales/ru.yml` - добавить переводы:
   - `telegram.commands.clients.menu.*`
   - `telegram.commands.clients.create.*`
   - `telegram.commands.clients.rename.*`
   - `telegram.commands.clients.delete.*`
3. Тесты: `spec/commands/clients_command_spec.rb` (обновить)
4. Миграция не требуется (модель Client существует)

**Формат переводов:**
```yaml
telegram:
  commands:
    clients:
      title: "Ваши клиенты:"
      add_button: "➕ Добавить клиента"
      back_button: "⬅️ Назад"
      menu:
        title: "Клиент: %{name} (%{key})\nПроектов: %{count}"
        rename_button: "✏️ Переименовать"
        projects_button: "📋 Показать проекты"
        delete_button: "🗑️ Удалить"
      create:
        enter_name: "Введите имя нового клиента (или 'cancel' для отмены):"
        enter_key: "Введите ключ (2-50 символов, a-z0-9_- ) или нажмите кнопку:"
        use_suggested: "✅ Использовать: %{key}"
        success: "Клиент '%{name}' создан с ключом '%{key}'"
        cancelled: "Создание клиента отменено"
      rename:
        enter_name: "Введите новое имя для клиента %{current_name}:"
        success: "Клиент переименован: %{old_name} → %{new_name}"
        cancelled: "Переименование отменено"
      delete:
        confirm: "Удалить клиента %{name} (%{key})?"
        success: "Клиент удален"
        error_has_projects: "Нельзя удалить: у клиента есть проекты"
      projects:
        title: "Проекты клиента %{name} (%{key}):"
        empty: "Нет проектов"
```

### 11. Рабочий процесс разработки с git worktree

Для изоляции разработки интерактивной команды `/clients` используем git worktree:

```bash
# Создание новой ветки и рабочей директории
cd /home/danil/code/mashtime.ru
git worktree add -b feature/interactive-clients ~/code/mashtime-clients HEAD

# Переход в новую рабочую директорию
cd ~/code/mashtime-clients

# Установка зависимостей
make deps
bun install

# Запуск приложения с отдельным тестовым ботом
# Создать .env.local с тестовыми токенами
./bin/dev

# После завершения разработки
# Удаление worktree
cd /home/danil/code/mashtime.ru
git worktree remove ~/code/mashtime-clients
# Если нужно также удалить ветку
git branch -d feature/interactive-clients
```

**Преимущества worktree:**
- Изолированная среда разработки без переключения веток
- Можно одновременно работать над несколькими задачами
- Быстрое переключение между ветками (просто смена директорий)
- Конфигурация и окружение могут отличаться (тестовый бот, отдельная БД)

**Настройка окружения для тестирования:**
```bash
# В ~/code/mashtime-clients/.env.local
TELEGRAM_BOT_TOKEN=your_test_token_dev_bot
DATABASE_URL=postgres://localhost/mashtime_clients_dev
REDIS_URL=redis://localhost:6380
```

Эта спецификация сохраняет логику и UX команды /projects, адаптируя её для управления клиентами, с полной обратной совместимостью существующего функционала.
