# Спецификация: Удаление поля `name` из модели Project

**Дата создания:** 2025-11-16
**Статус:** В разработке
**Автор:** Команда разработки

---

## 1. Обзор

### 1.1 Текущее состояние
Модель `Project` имеет два поля для идентификации и отображения:
- `name` (string) - "красивое" название проекта (например, "Work Project")
- `slug` (string, not null) - URL-friendly идентификатор (например, "work-project")

Оба поля имеют:
- Уникальные индексы в БД
- Валидацию на уникальность в модели
- `friendly_id` использует `name` для автогенерации `slug`

### 1.2 Проблематика
1. **Дублирование данных**: два поля хранят практически одинаковую информацию
2. **Путаница**: непонятно, какое поле использовать для отображения
3. **Сложность переименования**: требуется логика для изменения обоих полей или одного из них
4. **Дополнительный код**: больше валидаций, больше логики генерации

### 1.3 Целевое состояние
- Остается только поле `slug` как единственный идентификатор проекта
- `slug` используется и для URL, и для отображения в UI
- `friendly_id` настроен на использование самого `slug` (без автогенерации из другого поля)
- Упрощена логика создания и переименования проектов

---

## 2. Технические изменения

### 2.1 База данных

#### Схема до миграции:
```ruby
create_table "projects" do |t|
  t.string "name"                          # УДАЛИТЬ
  t.string "slug", null: false
  t.text "description"
  t.boolean "active", default: true, null: false
  t.integer "telegram_chat_id"
  t.bigint "client_id"
  # ...
end

add_index :projects, :name, unique: true  # УДАЛИТЬ
add_index :projects, :slug, unique: true
```

#### Схема после миграции:
```ruby
create_table "projects" do |t|
  t.string "slug", null: false
  t.text "description"
  t.boolean "active", default: true, null: false
  t.integer "telegram_chat_id"
  t.bigint "client_id"
  # ...
end

add_index :projects, :slug, unique: true
```

### 2.2 Модель Project

#### До:
```ruby
friendly_id :name, use: :slugged
validates :name, presence: true, uniqueness: true
scope :ordered, -> { order(:name) }

def should_generate_new_friendly_id?
  name_changed?
end
```

#### После:
```ruby
friendly_id :slug, use: :slugged
scope :ordered, -> { order(:slug) }

# Методы автогенерации slug больше не нужны
```

### 2.3 Затронутые компоненты

**Commands (Telegram bot):**
- `projects_command.rb` - основная логика управления проектами
- `add_command.rb` - добавление времени
- `edit_command.rb` - редактирование записей
- `users_command.rb` - управление пользователями
- `rate_command.rb` - управление ставками
- `clients_command.rb` - отображение проектов клиента

**Services:**
- `telegram/time_shift_operations_service.rb` - операции с временем

**Jobs & Mailers:**
- `project_member_notification_job.rb` - уведомления о новых участниках
- `invite_mailer.rb` - письма с приглашениями

**Views:**
- `memberships/index.html.haml` - список участников проекта
- `projects/_projects.html.haml` - список проектов
- `invite_mailer/*.haml` - email шаблоны

**Tests:**
- `spec/fixtures/projects.yml` - все 50+ проектов в fixtures
- `spec/controllers/telegram/webhook/projects_command_spec.rb` - тесты команды
- `spec/services/reporter_spec.rb` - тесты отчетов
- `spec/fixtures/fixture_test_spec.rb` - базовые тесты fixtures

---

## 3. Изменения в пользовательском интерфейсе

### 3.1 Telegram Bot

#### Создание проекта (интерактивное)

**ДО:**
```
Бот: Введите название проекта:
Пользователь: My Awesome Project
Бот: ✅ Создан проект 'My Awesome Project' (my-awesome-project)
```

**ПОСЛЕ:**
```
Бот: Введите slug (идентификатор) проекта:
Пользователь: my-awesome-project
Бот: ✅ Создан проект `my-awesome-project`
```

#### Создание проекта (legacy команда)

**ДО и ПОСЛЕ (без изменений):**
```
/projects create my-project
✅ Создан проект `my-project`
```

#### Переименование проекта

**ДО:**
```
[Проект: Work Project (work-project)]
Меню:
- Переименовать название  → вводится новое название
- Переименовать slug       → вводится новый slug
- Переименовать оба        → вводится название и slug
```

**ПОСЛЕ:**
```
[Проект: work-project]
Меню:
- Переименовать проект → вводится новый slug
```

#### Отображение в списках и сообщениях

**ДО:**
```
Отметили в Work Project 2 часа
Проект 'Work Project' (work-project)
```

**ПОСЛЕ:**
```
Отметили в work-project 2 часа
Проект `work-project`
```

### 3.2 Web интерфейс

**ДО:**
```
Work Project
  work-project
```

**ПОСЛЕ:**
```
work-project
```

---

## 4. Обратная совместимость

### 4.1 Миграция данных
- **Данные не теряются**: все проекты уже имеют поле `slug`
- **Rollback возможен**: можно восстановить `name` из `slug` (beautify slug)

### 4.2 API/URL
- **Без изменений**: все URL используют slug, не name
- **Без breaking changes**: внешние интеграции не затронуты

### 4.3 Legacy команды
- `/projects create slug` - работает как и раньше
- Все команды с параметром `project_slug` - без изменений

---

## 5. Риски и ограничения

### 5.1 UX-риски

**Риск:** Пользователям менее удобно вводить slug вместо красивого названия

**Пример:**
- ДО: пользователь вводит "My Awesome Project" → автоматически создается slug
- ПОСЛЕ: пользователь должен сразу ввести "my-awesome-project"

**Митигация:**
- В подсказках бота указывать формат slug: "Только латиница, цифры и дефисы"
- Добавить примеры правильных slug
- Сохранить валидацию slug (15 символов, латиница, дефисы)

### 5.2 Читаемость

**Риск:** Slug менее читаем в UI

**Пример:**
- ДО: "Work Project" - легко читать
- ПОСЛЕ: "work-project" - менее естественно

**Митигация:**
- Это design decision - slug достаточно читаем для идентификации
- В будущем можно добавить display_name как опциональное поле (если потребуется)

### 5.3 Технические риски

**Низкие риски:**
- Миграция БД - простая (удаление колонки)
- Откат - возможен (slug содержит всю информацию)
- Тесты - покрывают все изменения

---

## 6. Метрики успеха

### 6.1 Технические метрики
- ✅ Все тесты проходят после миграции
- ✅ Удалено 100+ строк кода (валидации, генерация slug, логика переименования)
- ✅ Уменьшено время создания проекта (меньше валидаций)
- ✅ Упрощена модель Project

### 6.2 Бизнес-метрики
- 📊 Количество созданных проектов до/после (не должно упасть)
- 📊 Количество ошибок при создании проектов
- 📊 Время создания проекта пользователем

---

## 7. План отката

### Если нужно вернуть поле `name`:

1. **Миграция БД:**
```ruby
add_column :projects, :name, :string
add_index :projects, :name, unique: true

Project.find_each do |project|
  project.update_column(:name, project.slug.titleize)
end
```

2. **Модель:** вернуть `friendly_id :name`
3. **Код:** вернуть все замены `slug` → `name`
4. **Тесты:** вернуть fixtures с полем `name`

**Время отката:** ~2 часа

---

## 8. Зависимости

### Нет зависимостей от других задач

Это standalone-изменение, не блокирует и не блокируется другими задачами.

---

## 9. Acceptance Criteria

- [ ] Удалена колонка `name` из таблицы `projects`
- [ ] Удален индекс `index_projects_on_name`
- [ ] Модель `Project` использует `friendly_id :slug`
- [ ] Все команды используют `project.slug` вместо `project.name`
- [ ] Все view используют `project.slug`
- [ ] Все fixtures обновлены (удалено поле `name`)
- [ ] Все тесты проходят
- [ ] I18n переводы обновлены (убраны упоминания "название")
- [ ] Логика переименования упрощена (только slug)
- [ ] Telegram бот работает корректно с новым UI

---

## 10. Дополнительная информация

### Альтернативные решения

**Альтернатива 1:** Оставить оба поля
- ❌ Не решает проблему дублирования
- ❌ Сохраняет сложность

**Альтернатива 2:** Удалить `slug`, оставить `name`
- ❌ Проблемы с URL (пробелы, кириллица)
- ❌ Нарушает существующие URL

**Выбранное решение:** Удалить `name`, оставить `slug`
- ✅ Один источник истины
- ✅ URL-friendly
- ✅ Упрощение кода

---

**Приложения:**
- План реализации: `remove_project_name_field_specification_plan.md`
