# Спецификация: Улучшение управления проектами в команде /projects

## Метаданные
- **Тип**: Feature Enhancement
- **Статус**: В разработке
- **Дата создания**: 2025-11-12
- **Команда**: `/projects`
- **Приоритет**: Средний

## Обзор

Расширение функциональности команды `/projects` для предоставления более удобного интерфейса управления проектами через Telegram бот с использованием inline-клавиатур и интерактивного меню.

## Текущее состояние

### Существующая функциональность
- `/projects` - показывает текстовый список проектов
- `/projects create project-slug` - создание проекта с явным slug
- `/projects create` - интерактивное создание с запросом slug

### Ограничения текущей реализации
- Отсутствие визуального интерфейса для выбора проектов
- Невозможность редактирования существующих проектов
- Отсутствие управления клиентами проекта
- Нет функции удаления проектов
- Slug должен указываться пользователем вручную

## Новая функциональность

### 1. Главное меню проектов

**Триггер**: `/projects`

**Поведение**:
```
Отображается сообщение:
"📁 Ваши проекты"

Inline-клавиатура:
┌─────────────────────────────┐
│  ➕ Добавить проект          │
├─────────────────────────────┤
│ proj-a    │ proj-b    │ ... │
│ proj-d    │ proj-e    │ ... │
│ ...       │ ...       │ ... │
└─────────────────────────────┘
```

**Требования к реализации**:
- Кнопка "➕ Добавить проект" всегда первая, занимает всю ширину
- Проекты отображаются кнопками в 3 столбца
- **Порядок проектов**: по алфавиту (slug в алфавитном порядке)
- Максимум видимых проектов: первые 30 (ограничение Telegram API на inline-кнопки)
- **Текст кнопки проекта**: slug проекта (максимум 15 символов, обрезается с "...")

**Callback data**:
- Добавить проект: `projects:create`
- Выбор проекта: `projects:select:{project_slug}`

### 2. Создание проекта

**Триггер**: Нажатие на "➕ Добавить проект" (callback `projects:create`)

**Шаг 1: Запрос названия**
```
📝 Введите название проекта:

(или 'cancel' для отмены)
```

**Валидация названия**:
- Минимальная длина: 1 символ
- Максимальная длина: 100 символов
- Допустимые символы: любые Unicode-символы
- Отмена: "cancel" (регистронезависимо)

**Шаг 2: Генерация slug и создание**

**Алгоритм генерации slug**:
1. Транслитерация названия в латиницу (используя существующий метод или gem 'babosa')
2. Приведение к нижнему регистру
3. Замена пробелов и небуквенно-цифровых символов на дефис
4. Удаление множественных дефисов подряд
5. Удаление дефисов в начале и конце
6. **Максимальная длина: 15 символов**

**Проверка уникальности slug**:
- Если slug уникален → создание проекта
- Если slug НЕ уникален → добавление суффикса `-{номер}`
  - Проверяются `-1`, `-2`, `-3`, ... пока не найдется уникальный
  - Максимум попыток: 100
  - **Важно**: итоговая длина slug с суффиксом не должна превышать 15 символов

**Ответ при успехе**:
```
✅ Создан проект "{название}"
🔑 Идентификатор: {slug}

[Возврат в меню проектов]
```

**Ответ при ошибке**:
```
❌ Не удалось создать проект: {причина}

[Возврат в меню проектов]
```

**Возможные ошибки**:
- Название слишком короткое/длинное
- Не удалось сгенерировать уникальный slug (после 100 попыток)
- Ошибка сохранения в БД

### 3. Меню управления проектом

**Триггер**: Нажатие на проект (callback `projects:select:{project_slug}`)

**Условие доступа**: Пользователь должен быть `owner` проекта

**Отображение**:
```
📁 Проект: {название}
🔑 Идентификатор: {slug}
👤 Клиент: {client_name или "Не указан"}

Inline-клавиатура:
┌─────────────────────────────┐
│ ✏️ Переименовать             │
├─────────────────────────────┤
│ 🏢 Клиент                    │
├─────────────────────────────┤
│ 🗑️ Удалить проект            │
├─────────────────────────────┤
│ ◀️ Назад к списку            │
└─────────────────────────────┘
```

**Callback data**:
- Переименовать: `projects:rename:{project_slug}`
- Клиент: `projects:client:{project_slug}`
- Удалить: `projects:delete:{project_slug}`
- Назад: `projects:list`

**Для не-owner пользователей**:
```
📁 Проект: {название}
🔑 Идентификатор: {slug}
👤 Клиент: {client_name или "Не указан"}

ℹ️ Только владелец может управлять проектом

[◀️ Назад к списку]
```

### 4. Переименование проекта

**Триггер**: Нажатие на "✏️ Переименовать" (callback `projects:rename:{project_slug}`)

**Шаг 1: Выбор типа изменений**
```
📁 Проект: {название}

Выберите, что хотите изменить:

┌─────────────────────────────┐
│ 📝 Название                  │
├─────────────────────────────┤
│ 🔑 Идентификатор (slug)      │
├─────────────────────────────┤
│ ✏️ Название и идентификатор  │
├─────────────────────────────┤
│ ◀️ Отмена                    │
└─────────────────────────────┘
```

**Callback data**:
- Название: `projects:rename_title:{project_slug}`
- Slug: `projects:rename_slug:{project_slug}`
- Оба: `projects:rename_both:{project_slug}`
- Отмена: `projects:select:{project_slug}`

#### 4.1. Изменение только названия

**Запрос**:
```
📝 Введите новое название проекта:

Текущее: {current_name}

(или 'cancel' для отмены)
```

**Валидация**: Аналогична созданию проекта

**Ответ при успехе**:
```
✅ Название проекта изменено
Старое: {old_name}
Новое: {new_name}

[Возврат в меню проекта]
```

#### 4.2. Изменение только slug

**Запрос**:
```
🔑 Введите новый идентификатор (slug):

Текущий: {current_slug}

⚠️ Внимание: изменение идентификатора может повлиять на интеграции

(или 'cancel' для отмены)
```

**Валидация**:
- Минимальная длина: 1 символ
- **Максимальная длина: 15 символов**
- Допустимые символы: латинские буквы, цифры, дефис
- Не может начинаться или заканчиваться дефисом
- Уникальность в рамках всех проектов

**Ответ при успехе**:
```
✅ Идентификатор проекта изменен
Старый: {old_slug}
Новый: {new_slug}

[Возврат в меню проекта]
```

**Ответ при ошибке уникальности**:
```
❌ Идентификатор "{slug}" уже занят
Попробуйте другой вариант

[Возврат в меню проекта]
```

#### 4.3. Изменение названия и slug

**Шаг 1: Запрос названия**
```
📝 Введите новое название проекта:

Текущее: {current_name}

(или 'cancel' для отмены)
```

**Шаг 2: Запрос slug**
```
🔑 Введите новый идентификатор (slug):

Текущий: {current_slug}
Предложенный (на основе названия): {suggested_slug}

⚠️ Нажмите "Использовать предложенный" или введите свой вариант

┌─────────────────────────────┐
│ ✅ Использовать предложенный │
└─────────────────────────────┘

(или 'cancel' для отмены)
```

**Callback data**:
- Использовать предложенный: `projects:rename_use_suggested:{project_slug}:{suggested_slug}`

**Ответ при успехе**:
```
✅ Проект успешно переименован

Название: {old_name} → {new_name}
Идентификатор: {old_slug} → {new_slug}

[Возврат в меню проекта]
```

### 5. Управление клиентом

**Триггер**: Нажатие на "🏢 Клиент" (callback `projects:client:{project_slug}`)

**Отображение**:
```
👤 Клиент проекта "{project_name}"

Текущий клиент: {client_name или "Не указан"}

┌─────────────────────────────┐
│ ✏️ Изменить клиента          │
├─────────────────────────────┤
│ 🗑️ Удалить клиента           │  (если установлен)
├─────────────────────────────┤
│ ◀️ Назад к проекту           │
└─────────────────────────────┘
```

**Callback data**:
- Изменить: `projects:client_edit:{project_slug}`
- Удалить: `projects:client_delete:{project_slug}`
- Назад: `projects:select:{project_slug}`

#### 5.1. Установка/изменение клиента

**Триггер**: Нажатие на "✏️ Изменить клиента" (callback `projects:client_edit:{project_slug}`)

**Запрос**:
```
👤 Введите название клиента:

Текущий: {current_client_name или "Не указан"}

(или 'cancel' для отмены)
```

**Валидация**:
- Минимальная длина: 1 символ
- Максимальная длина: 100 символов
- Допустимые символы: любые Unicode-символы
- Отмена: "cancel" (регистронезависимо)

**Ответ при успехе**:
```
✅ Клиент проекта обновлен
{old_client_name или "Не указан"} → {new_client_name}

[Возврат в меню клиента]
```

#### 5.2. Удаление клиента

**Триггер**: Нажатие на "🗑️ Удалить клиента" (callback `projects:client_delete:{project_slug}`)

**Подтверждение**:
```
⚠️ Удалить клиента "{client_name}"?

┌─────────────────────────────┐
│ ✅ Да, удалить               │
├─────────────────────────────┤
│ ❌ Отмена                    │
└─────────────────────────────┘
```

**Callback data**:
- Подтвердить: `projects:client_delete_confirm:{project_slug}`
- Отмена: `projects:client:{project_slug}`

**Ответ при успехе**:
```
✅ Клиент удален

[Возврат в меню клиента]
```

### 6. Удаление проекта

**Триггер**: Нажатие на "🗑️ Удалить проект" (callback `projects:delete:{project_slug}`)

**Подтверждение (шаг 1)**:
```
⚠️ Удаление проекта "{project_name}"

Будут удалены:
• Все записи времени ({time_shifts_count})
• Все участники ({memberships_count})
• Все приглашения ({invites_count})

❗ Это действие необратимо!

┌─────────────────────────────┐
│ 🗑️ Да, удалить все           │
├─────────────────────────────┤
│ ❌ Отмена                    │
└─────────────────────────────┘
```

**Callback data**:
- Подтвердить: `projects:delete_confirm:{project_slug}`
- Отмена: `projects:select:{project_slug}`

**Подтверждение (шаг 2 - финальное)**:
```
🚨 ФИНАЛЬНОЕ ПОДТВЕРЖДЕНИЕ

Вы уверены, что хотите удалить проект "{project_name}"?

Введите название проекта для подтверждения:
"{project_name}"

(или 'cancel' для отмены)
```

**Валидация**:
- Точное совпадение с названием проекта (регистрозависимо)
- Отмена: "cancel" (регистронезависимо)

**Ответ при успехе**:
```
✅ Проект "{project_name}" успешно удален

[Возврат в меню проектов]
```

**Ответ при неверном подтверждении**:
```
❌ Название не совпадает
Удаление отменено

[Возврат в меню проекта]
```

## Технические детали

### Изменения в моделях

#### Project
```ruby
# Новые методы
def generate_unique_slug(base_name)
  # Генерация slug из названия
  # Обеспечение уникальности
  # Максимальная длина: 15 символов
end

def can_be_managed_by?(user)
  # Проверка прав владельца
  memberships.where(user: user, role: 'owner').exists?
end

def deletion_stats
  # Статистика для отображения при удалении
  {
    time_shifts_count: time_shifts.count,
    memberships_count: memberships.count,
    invites_count: invites.count
  }
end

# Scope для сортировки
scope :alphabetically, -> { order(slug: :asc) }
```

### Изменения в контроллере

#### app/controllers/telegram/webhook_controller.rb
```ruby
# Новые callback_query обработчики
def handle_projects_callback
  case callback_data
  when /^projects:create$/
    start_project_creation
  when /^projects:select:(.+)$/
    show_project_menu($1)
  when /^projects:list$/
    show_projects_list
  when /^projects:rename:(.+)$/
    show_rename_menu($1)
  when /^projects:client:(.+)$/
    show_client_menu($1)
  when /^projects:delete:(.+)$/
    confirm_project_deletion($1)
  # ... и другие варианты
  end
end
```

### Новая команда

#### app/commands/projects_command.rb
```ruby
class ProjectsCommand < BaseCommand
  CALLBACK_PREFIX = 'projects'.freeze

  def call!(*args)
    if args.empty?
      show_projects_list
    else
      # Обратная совместимость со старым форматом
      handle_legacy_create_format(args)
    end
  end

  def show_projects_list
    # Отображение главного меню
    # Проекты сортируются по алфавиту (slug)
    projects = current_user.projects.alphabetically.limit(30)
  end

  def handle_callback_query(data)
    # Обработка всех callback
  end

  private

  def create_project(name)
    # Создание проекта с генерацией slug (макс 15 символов)
  end

  def show_project_menu(slug)
    # Меню управления проектом
  end

  def rename_project(slug)
    # Переименование
  end

  def manage_client(slug)
    # Управление клиентом
  end

  def delete_project(slug)
    # Удаление проекта
  end

  # ... другие методы
end
```

### Контекст сессии

**Ключи для save_context**:
```ruby
CONTEXT_AWAITING_PROJECT_NAME = :awaiting_project_name
CONTEXT_AWAITING_RENAME_TITLE = :awaiting_rename_title
CONTEXT_AWAITING_RENAME_SLUG = :awaiting_rename_slug
CONTEXT_AWAITING_RENAME_BOTH = :awaiting_rename_both
CONTEXT_AWAITING_CLIENT_NAME = :awaiting_client_name
CONTEXT_AWAITING_DELETE_CONFIRM = :awaiting_delete_confirm
CONTEXT_CURRENT_PROJECT = :current_project_slug
```

### Локализация

#### config/locales/ru.yml
```yaml
ru:
  commands:
    projects:
      title: "📁 Ваши проекты"
      add_button: "➕ Добавить проект"
      back_button: "◀️ Назад к списку"
      cancel_button: "❌ Отмена"

      create:
        enter_name: "📝 Введите название проекта:\n\n(или 'cancel' для отмены)"
        success: "✅ Создан проект \"%{name}\"\n🔑 Идентификатор: %{slug}"
        cancelled: "Создание проекта отменено"
        error: "❌ Не удалось создать проект: %{reason}"

      menu:
        title: "📁 Проект: %{name}\n🔑 Идентификатор: %{slug}\n👤 Клиент: %{client}"
        no_client: "Не указан"
        rename_button: "✏️ Переименовать"
        client_button: "🏢 Клиент"
        delete_button: "🗑️ Удалить проект"
        owner_only: "ℹ️ Только владелец может управлять проектом"

      rename:
        # ... и т.д.
```

## Обратная совместимость

**Сохраняется поддержка старого формата**:
- `/projects` - новый интерфейс
- `/projects create slug` - прямое создание (deprecated)
- `/projects create` - интерактивное создание (deprecated)

**Migration path**:
1. В первой версии оба формата работают
2. Показывать подсказку о новом формате при использовании старого
3. Через 3 месяца можно удалить поддержку старого формата

## Тестирование

### Основные сценарии

1. **Просмотр списка проектов**
   - Отображение кнопки добавления
   - Отображение проектов в 3 столбца в алфавитном порядке
   - Ограничение на 30 проектов

2. **Создание проекта**
   - Генерация уникального slug (макс 15 символов)
   - Валидация названия
   - Добавление суффикса при конфликте

3. **Переименование проекта**
   - Изменение только названия
   - Изменение только slug (макс 15 символов)
   - Изменение обоих
   - Проверка уникальности slug

4. **Управление клиентом**
   - Установка клиента
   - Изменение клиента
   - Удаление клиента

5. **Удаление проекта**
   - Первое подтверждение
   - Финальное подтверждение с названием
   - Проверка прав доступа

### Граничные случаи

- Проект без членства (не должен отображаться)
- Более 30 проектов (должны отображаться первые 30 в алфавитном порядке)
- Конфликт slug (должен добавляться суффикс с учетом лимита 15 символов)
- Попытка удаления не-owner пользователем
- Отмена на любом этапе
- Slug длиннее 15 символов (должен обрезаться)

## Безопасность

### Проверки доступа
- Все операции управления проектом требуют роли `owner`
- Просмотр доступен всем участникам
- Callback data не содержит чувствительной информации

### Валидация входных данных
- Длина названий и slug (slug макс 15 символов)
- Символы в slug (только разрешенные)
- Уникальность slug
- XSS-защита (экранирование в сообщениях)

## Производительность

### Оптимизации
- Кэширование списка проектов пользователя
- Eager loading для memberships
- Индексы на slug для быстрой проверки уникальности
- Ограничение на 30 видимых проектов
- Сортировка по slug в БД (индекс на slug)

## Мониторинг

### Метрики
- Количество созданий проектов в день
- Количество переименований
- Количество удалений
- Среднее время создания проекта
- Количество конфликтов slug

### Логирование
- Все операции с проектами (создание, изменение, удаление)
- Неудачные попытки (валидация, права доступа)
- Ошибки при генерации slug

## Развертывание

### Миграции
Не требуется новых миграций, используются существующие поля модели `Project`.

### Откат
При необходимости отката можно временно вернуться к старой команде, переименовав новую.

## Дальнейшее развитие

### Возможные улучшения (вне scope текущей спецификации)
- Пагинация для проектов (если больше 30)
- Фильтрация проектов (по клиенту, по активности)
- Массовые операции (архивация, удаление нескольких)
- Экспорт/импорт проектов
- История изменений проекта
- Шаблоны проектов

## Приложения

### A. Примеры генерации slug (макс 15 символов)

| Название проекта | Сгенерированный slug | Комментарий |
|-----------------|---------------------|-------------|
| "Мой проект" | `moy-proekt` | Транслитерация |
| "Project 123" | `project-123` | Уже латиница |
| "Test!!Project" | `test-project` | Удаление спецсимволов |
| "  spaces  " | `spaces` | Удаление лишних пробелов |
| "Очень длинное название" | `ochen-dlinnoe` | Обрезка до 15 символов |
| "Very Long Project Name" | `very-long-proj` | Обрезка до 15 символов |
| "Test" (существует) | `test-1` | Добавление суффикса |
| "Test" (test-1 существует) | `test-2` | Инкремент суффикса |
| "Long Name Test" (slug занят) | `long-name-te-1` | Обрезка с учетом суффикса |

### B. Callback data схема

```
projects:create                           # Создать проект
projects:select:{slug}                    # Выбрать проект
projects:list                             # Список проектов
projects:rename:{slug}                    # Меню переименования
projects:rename_title:{slug}              # Переименовать название
projects:rename_slug:{slug}               # Переименовать slug
projects:rename_both:{slug}               # Переименовать оба
projects:rename_use_suggested:{slug}:{suggested} # Использовать предложенный
projects:client:{slug}                    # Меню клиента
projects:client_edit:{slug}               # Изменить клиента
projects:client_delete:{slug}             # Удалить клиента
projects:client_delete_confirm:{slug}     # Подтвердить удаление клиента
projects:delete:{slug}                    # Удалить проект
projects:delete_confirm:{slug}            # Подтвердить удаление проекта
```

## Критерии приемки

### Обязательные требования
- ✅ Все функции реализованы согласно спецификации
- ✅ Написаны тесты для всех сценариев
- ✅ Добавлены локализации для всех сообщений
- ✅ Проверка прав доступа работает корректно
- ✅ Генерация slug работает и обеспечивает уникальность (макс 15 символов)
- ✅ Проекты отображаются в алфавитном порядке по slug
- ✅ Текст кнопок - slug проекта
- ✅ Обратная совместимость со старым форматом сохранена
- ✅ Код соответствует стандартам проекта (RuboCop)
- ✅ Нет регрессий в существующей функциональности

### Проверка качества
- Все тесты проходят (`make test`)
- Rubocop проходит без ошибок (`bundle exec rubocop`)
- Brakeman не находит проблем безопасности (`bundle exec brakeman`)
- Документация обновлена (README, CHANGELOG)

## Зависимости

### Внешние библиотеки
- `babosa` - для транслитерации названий в slug (опционально, можно использовать встроенные методы Rails)

### Изменения в других компонентах
- Отсутствуют (изолированное изменение в команде /projects)
