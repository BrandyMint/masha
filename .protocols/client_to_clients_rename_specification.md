# Спецификация переименования команды /client → /clients

## Обзор

Данная спецификация описывает процесс переименования Telegram-команды `/client` в `/clients` для соответствия общепринятым REST-подобным соглашениям именования и улучшения пользовательского опыта.

## Термины и определения

- **Команда** - метод в Telegram-боте с восклицательным знаком (например, `client!`)
- **Алиас команды** - альтернативное имя для вызова той же команды
- **Контекст команды** - состояние сессии пользователя при интерактивном взаимодействии с ботом
- **Обратная совместимость** - поддержка старого названия команды `/client` после переименования

## Причины переименования

### 1. Соответствие REST-соглашениям
Команда управляет **множественными** клиентами (список, создание, редактирование нескольких клиентов), поэтому логично использовать множественное число `/clients`, аналогично команде `/projects`.

### 2. Единообразие с другими командами
- `/projects` - управление несколькими проектами ✅
- `/client` - управление несколькими клиентами ❌ (несоответствие)
- `/clients` - управление несколькими клиентами ✅ (исправлено)

### 3. Улучшение пользовательского опыта
Пользователи ожидают единообразия в командах. Множественное число сигнализирует о работе с коллекцией объектов.

### 4. Предотвращение путаницы
Текущее название `/client` может восприниматься как команда для работы с **одним конкретным** клиентом, в то время как она управляет **списком** клиентов.

## Функциональные требования

### Основное переименование

**Было:**
```
/client - показать список клиентов
/client add - создать клиента
/client show {key} - показать информацию о клиенте
/client edit {key} - редактировать клиента
/client delete {key} - удалить клиента
/client projects {key} - показать проекты клиента
/client help - справка
```

**Станет:**
```
/clients - показать список клиентов
/clients add - создать клиента
/clients show {key} - показать информацию о клиенте
/clients edit {key} - редактировать клиента
/clients delete {key} - удалить клиента
/clients projects {key} - показать проекты клиента
/clients help - справка
```

**Примечание:** Команды `attach` и `detach` удалены из функционала и не включены в переименование.

### Обратная совместимость

**Требование:** Старая команда `/client` должна **продолжать работать** как алиас для `/clients` в течение переходного периода.

**Поведение:**
- При вызове `/client` бот показывает **предупреждение** о переименовании и выполняет команду
- Предупреждение: `⚠️ Команда /client переименована в /clients. Пожалуйста, используйте /clients в будущем.`
- После предупреждения команда работает полностью идентично `/clients`

## Технические требования

### 1. Переименование класса команды

**Было:**
```ruby
class ClientCommand < BaseCommand
  # ...
end
```

**Станет:**
```ruby
class ClientsCommand < BaseCommand
  # ...
end
```

### 2. Обновление констант контекстов

**Было (в BaseCommand):**
```ruby
# ClientCommand contexts
ADD_CLIENT_NAME = :add_client_name
ADD_CLIENT_KEY = :add_client_key
EDIT_CLIENT_NAME = :edit_client_name
```

**Станет:**
```ruby
# ClientsCommand contexts
ADD_CLIENT_NAME = :add_client_name
ADD_CLIENT_KEY = :add_client_key
EDIT_CLIENT_NAME = :edit_client_name
```

**Примечание:** Сами константы **НЕ переименовываются**, так как это внутренние идентификаторы контекстов, используемые в сессиях. Переименование констант нарушит активные сессии пользователей.

### 3. Обновление локализации

**Файл:** `config/locales/ru.yml`

**Было:**
```yaml
telegram:
  commands:
    client:
      help_title: "🏢 Управление клиентами (компаниями)"
      help_list: "• /client - показать список ваших клиентов"
      help_add: "• /client add - создать нового клиента"
      # ... остальные ключи
```

**Станет:**
```yaml
telegram:
  commands:
    clients:
      help_title: "🏢 Управление клиентами (компаниями)"
      help_list: "• /clients - показать список ваших клиентов"
      help_add: "• /clients add - создать нового клиента"
      # ... остальные ключи
      deprecated_warning: "⚠️ Команда /client переименована в /clients. Пожалуйста, используйте /clients в будущем."
```

### 4. Создание алиаса для обратной совместимости

**Подход:** Создать отдельный класс `ClientCommand` (устаревший алиас), который наследуется от `ClientsCommand` и добавляет предупреждение.

**Реализация:**
```ruby
# app/commands/client_command.rb (устаревший алиас)
class ClientCommand < ClientsCommand
  def call(*args)
    # Показываем предупреждение о переименовании
    respond_with :message, text: t('telegram.commands.clients.deprecated_warning')

    # Вызываем родительский метод
    super
  end
end
```

### 5. Обновление тестов

**Файл:** `spec/controllers/telegram/webhook/client_command_spec.rb`

**Действия:**
1. Переименовать файл в `clients_command_spec.rb`
2. Обновить все вызовы команды с `:client` на `:clients`
3. Добавить отдельные тесты для алиаса `/client` (проверка предупреждения)

**Пример обновления теста:**

**Было:**
```ruby
expect { dispatch_command :client }.not_to raise_error
```

**Станет:**
```ruby
expect { dispatch_command :clients }.not_to raise_error
```

**Новые тесты для алиаса:**
```ruby
context 'deprecated /client command' do
  it 'shows deprecation warning when using /client' do
    # Проверяем что команда работает
    expect { dispatch_command :client }.not_to raise_error

    # Можно добавить проверку наличия предупреждающего сообщения
    # если понадобится более строгая проверка
  end
end
```

### 6. Обновление документации

**Файлы для обновления:**
- `CLAUDE.md` - добавить раздел про `/clients` (заменить `/client`)
- `docs/development/telegram-bot-architecture.md` - обновить примеры
- `.protocols/client_command_specification.md` - переименовать и обновить
- `.serena/memories/` - обновить упоминания команды

## Миграционная стратегия

### Фаза 1: Подготовка (текущий момент)
- ✅ Создание спецификации
- ⏳ Создание плана реализации

### Фаза 2: Реализация
1. Переименование `ClientCommand` → `ClientsCommand`
2. Обновление локализации (`client:` → `clients:`)
3. Создание алиаса `ClientCommand` с предупреждением
4. Обновление тестов
5. Обновление документации

### Фаза 3: Тестирование
1. Запуск полного набора тестов
2. Проверка работы `/clients` команды
3. Проверка работы алиаса `/client` (с предупреждением)
4. Проверка всех подкоманд (add, show, edit, delete, projects, help)

### Фаза 4: Развертывание
1. Деплой изменений в production
2. Мониторинг использования команды
3. Отслеживание ошибок через Bugsnag

### Фаза 5: Поддержка устаревшего алиаса
**Срок:** 3-6 месяцев после релиза

- Алиас `/client` остается активным с предупреждением
- Пользователи постепенно переходят на `/clients`

### Фаза 6: Удаление алиаса (опционально)
**Срок:** Через 6+ месяцев после релиза

- Оценка использования алиаса `/client`
- Если использование минимальное (<5%), можно удалить алиас
- Альтернатива: оставить алиас навсегда (не создает технических проблем)

## План тестирования

### Unit-тесты

**Проверка основной команды `/clients`:**
- ✅ Список клиентов
- ✅ Создание клиента (add)
- ✅ Просмотр клиента (show)
- ✅ Редактирование клиента (edit)
- ✅ Удаление клиента (delete)
- ✅ Просмотр проектов клиента (projects)
- ✅ Справка (help)

**Проверка алиаса `/client`:**
- ✅ Команда работает идентично `/clients`
- ✅ Показывается предупреждение о переименовании
- ✅ Все подкоманды работают корректно

### Интеграционное тестирование

**Сценарий 1: Новый пользователь**
1. Пользователь вызывает `/clients` (первичное название)
2. Проверка корректности работы всех подкоманд
3. Проверка отображения справки

**Сценарий 2: Опытный пользователь (использует старое название)**
1. Пользователь вызывает `/client` (устаревшее)
2. Бот показывает предупреждение
3. Команда работает корректно
4. Пользователь привыкает к новому названию

**Сценарий 3: Интерактивный диалог**
1. Пользователь начинает создание клиента через `/clients add`
2. Проходит через контексты (add_client_name, add_client_key)
3. Клиент успешно создается
4. Проверка что контексты работают корректно

## Risks & Mitigation (Риски и их устранение)

### Риск 1: Нарушение активных сессий
**Описание:** Пользователи, находящиеся в процессе создания/редактирования клиента, могут столкнуться с ошибками.

**Устранение:**
- Константы контекстов **НЕ переименовываются** (остаются `ADD_CLIENT_NAME` и т.д.)
- Методы контекстов **НЕ переименовываются** (`add_client_name`, `add_client_key`, `edit_client_name`)
- Активные сессии продолжат работать без изменений

### Риск 2: Путаница пользователей
**Описание:** Пользователи могут не заметить переименования и продолжать использовать `/client`.

**Устранение:**
- Алиас `/client` продолжает работать с предупреждением
- Справка обновляется и показывает новое название
- Постепенный переход через предупреждения

### Риск 3: Документация устаревает
**Описание:** Старые ссылки на `/client` в документации, чатах, скриншотах.

**Устранение:**
- Полное обновление документации в рамках задачи
- Добавление примечания в CHANGELOG
- Обновление памяти Serena MCP

### Риск 4: Регрессии в тестах
**Описание:** Тесты могут начать падать после переименования.

**Устранение:**
- Использование фикстур (не создавать новые записи)
- Тщательное обновление всех вызовов `:client` → `:clients`
- Запуск полного набора тестов перед коммитом

## Чеклист реализации

### Код
- [ ] Переименовать `app/commands/client_command.rb` → `app/commands/clients_command.rb`
- [ ] Обновить класс `ClientCommand` → `ClientsCommand`
- [ ] Создать алиас `app/commands/client_command.rb` (deprecated) наследующийся от `ClientsCommand`
- [ ] Обновить все вызовы `t('telegram.commands.client.XXX')` → `t('telegram.commands.clients.XXX')`
- [ ] Удалить методы `handle_attach_project` и `handle_detach_project` из команды

### Локализация
- [ ] Переименовать секцию `client:` → `clients:` в `config/locales/ru.yml`
- [ ] Обновить все упоминания `/client` → `/clients` в текстах локализации
- [ ] Добавить ключ `deprecated_warning` для предупреждения
- [ ] Удалить ключи локализации для attach/detach команд

### Тесты
- [ ] Переименовать `spec/controllers/telegram/webhook/client_command_spec.rb` → `clients_command_spec.rb`
- [ ] Обновить все вызовы `dispatch_command :client` → `dispatch_command :clients`
- [ ] Добавить отдельный контекст для тестирования алиаса `/client`
- [ ] Запустить тесты и убедиться что все проходят

### Документация
- [ ] Обновить `CLAUDE.md` (заменить `/client` → `/clients`)
- [ ] Переименовать `.protocols/client_command_specification.md` → `clients_command_specification.md`
- [ ] Обновить содержимое спецификации с новым названием
- [ ] Обновить `docs/development/telegram-bot-architecture.md`
- [ ] Обновить `.serena/memories/` (если есть упоминания)

### Деплой
- [ ] Создать миграцию версии (semver bump)
- [ ] Добавить запись в CHANGELOG.md
- [ ] Создать git commit с понятным сообщением
- [ ] Запустить деплой в production
- [ ] Мониторинг ошибок через Bugsnag

## Примеры использования (после изменений)

### Просмотр списка клиентов
```
/clients

Ответ:
🏢 Ваши клиенты:
• acme-corp - ACME Corporation (3 проекта)
• techstart - TechStart Inc (1 проект)
```

### Создание нового клиента
```
/clients add

Бот: 📝 Введите название компании (юридическое наименование):
Пользователь: Global Solutions LLC

Бот: 🔑 Введите уникальный ключ (латиница, цифры, дефисы, подчеркивания):
Пользователь: global-solutions

Бот: ✅ Клиент 'Global Solutions LLC' (global-solutions) успешно создан!
```

### Использование устаревшего алиаса (обратная совместимость)
```
/client

Ответ:
⚠️ Команда /client переименована в /clients. Пожалуйста, используйте /clients в будущем.

🏢 Ваши клиенты:
• acme-corp - ACME Corporation (3 проекта)
• techstart - TechStart Inc (1 проект)
```

## Заключение

Переименование команды `/client` в `/clients` улучшит единообразие командного интерфейса Telegram-бота и соответствие REST-соглашениям. Использование алиаса для обратной совместимости обеспечит плавный переход для существующих пользователей без нарушения их опыта работы.

## Версионирование

- **Версия спецификации:** 1.0
- **Дата создания:** 2025-11-13
- **Автор:** Claude Code (на основе запроса пользователя)
- **Статус:** Черновик
