# Система автоматической генерации Changelog

Этот документ описывает настроенную систему автоматической генерации changelog для релизов проекта MashTime.

## 🎯 Как это работает

### Автоматическая генерация через GitHub Actions
При создании тега версии (например, `git tag v0.6.31`) автоматически запускается workflow:
1. Анализируются коммиты с последнего релиза
2. Генерируется человекочитаемый changelog с категоризацией
3. Создается релиз на GitHub с описанием
4. Автоматически запускается билд и деплой

### Локальная генерация через Makefile
```bash
# Показать changelog для последнего релиза
make changelog

# Предпросмотр changelog для текущих изменений
make changelog-preview

# Тестировать систему
make test-changelog
```

## 📝 Категории изменений

Система автоматически группирует коммиты по типам:

- ✨ **Новый функционал** - новые возможности и фичи
- 🐛 **Исправления** - исправленные ошибки и баги
- 🔧 **Улучшения** - рефакторинг и оптимизации
- 📚 **Документация** - обновления документации
- 📝 **Прочее** - остальные изменения

## 🏷️ Формат коммитов

Для лучшей классификации используйте стандартные префиксы:
- `feat:` или `Add` - для нового функционала
- `fix:` или `Fix` - для исправлений
- `refactor:` - для рефакторинга
- `docs:` - для документации
- `test:` - для тестов
- `chore:` - для технических задач

Примеры:
```
feat: Добавить экспорт времени в CSV
fix: Исправить отображение часов в Safari
refactor: Оптимизировать запросы к базе
docs: Обновить README с инструкциями
```

## 🚀 Процесс релиза

### Классический (через Makefile)
```bash
# Патч-релиз (увеличивает версию, создает changelog, пушит)
make release

# Минорный релиз
make minor-release
```

### Manual (через Git)
```bash
# 1. Создаем тег
git tag v0.6.31

# 2. Пушим тег (запустит автоматический релиз)
git push origin v0.6.31
```

## 📊 Пример сгенерированного changelog

```markdown
## Что нового в v0.6.31

### 🎯 Основные изменения с версии v0.6.30

#### ✨ Новый функционал
✨ Добавить экспорт времени в CSV
✨ Интеграция с Google Calendar

#### 🐛 Исправления
🐛 Исправить отображение часов в Safari
🐛 Починить обработку временных зон

#### 🔧 Улучшения
🔧 Оптимизировать запросы к базе

### 📊 Статистика релиза
- **Всего изменений:** 15 коммитов
- **Новых возможностей:** 3
- **Исправлений:** 8

---
🤖 *Changelog сгенерирован автоматически для MashTime Bot*
```

## 🛠️ Файлы системы

- `.github/workflows/release-simple.yml` - основной workflow
- `bin/generate_smart_changelog.sh` - локальный генератор changelog
- `Makefile` - цели для работы с changelog
- `docs/changelog-system.md` - эта документация

## 🔧 Настройка

Для изменения формата changelog:
1. Отредактируйте `bin/generate_smart_changelog.sh`
2. Измените правила категоризации
3. Обновите шаблоны вывода

Для изменения триггеров релиза:
1. Отредактируйте `.github/workflows/release-simple.yml`
2. Измените условия в секции `on:`

## 🐛 Устранение проблем

**Не работает changelog локально:**
```bash
chmod +x bin/generate_smart_changelog.sh
```

**Нет предыдущего тега:**
Система автоматически обработает первые 50 коммитов как начальный релиз.

**Кривая категоризация:**
Проверьте формат коммитов и при необходимости добавьте новые правила в скрипт.