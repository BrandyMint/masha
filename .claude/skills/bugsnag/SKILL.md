---
name: bugsnag-lookuper
description: Bugsnag API integration tool for viewing and managing errors. Fetches current errors, retrieves error context and stack traces, and marks errors as resolved. Uses BUGSNAG_DATA_API_KEY and BUGSNAG_PROJECT_ID from environment.
---

# Bugsnag Lookuper

Skill для интеграции с Bugsnag API, позволяющий просматривать и управлять ошибками в проекте.

## Возможности

- 📋 **Просмотр текущих ошибок** - Получение списка активных ошибок из Bugsnag
- 🔍 **Детальный контекст ошибки** - Просмотр полной информации об ошибке включая stack trace
- ✅ **Управление статусами** - Пометка ошибок как выполненные (resolved)
- 🔐 **Безопасная авторизация** - Использование API ключей из переменных окружения

## Переменные окружения

Для работы skill необходимы следующие переменные окружения:

```bash
export BUGSNAG_DATA_API_KEY="your_bugsnag_data_api_key"
export BUGSNAG_PROJECT_ID="your_bugsnag_project_id"
```

### Как получить API ключ

1. Перейдите в [Bugsnag Dashboard](https://app.bugsnag.com)
2. Настройки → Organization → API Authentication
3. Создайте Personal Access Token с правами на чтение ошибок
4. Получите ID проекта из настроек проекта

## Использование

### Просмотр всех активных ошибок
```bash
skill: "bugsnag-lookuper" "покажи все ошибки"
```

### Детальная информация об ошибке
```bash
skill: "bugsnag-lookuper" "покажи детали ошибки [error_id]"
```

### Пометка ошибки как выполненной
```bash
skill: "bugsnag-lookuper" "отметь ошибку [error_id] как выполненную"
```

### Комплексный анализ ошибок
```bash
skill: "bugsnag-lookuper" "проанализируй текущие ошибки и покажи самые критичные"
```

## Команды

- `show errors` / `list errors` - Показать список активных ошибок
- `show error <id>` - Показать детальную информацию об ошибке
- `resolve <id>` - Пометить ошибку как выполненную
- `analyze errors` - Проанализировать ошибки по критичности
- `help` - Показать справку

## Примеры работы

### 1. Получение списка ошибок
```
📋 Активные ошибки в проекте:
❌ Critical: DatabaseConnectionTimeout (15 событий) - ID: 5f8a9b2c
⚠️ Warning: InvalidJsonFormat (8 событий) - ID: 3d7e4f1a
❌ Error: UndefinedMethod (3 события) - ID: 9b2c8d5e
```

### 2. Детали ошибки
```
🔍 Ошибка: DatabaseConnectionTimeout
ID: 5f8a9b2c
Статус: Open
Событий: 15
Первое появление: 2025-01-09 10:30
Последнее: 2025-01-09 14:45

Stack trace:
├── app/models/database.rb:127 - in connection_timeout
├── app/services/data_processor.rb:45 - in process_data
├── app/controllers/api_controller.rb:23 - in index
└── app/jobs/background_job.rb:67 - in perform

Контекст:
• Rails Environment: production
• User Agent: Mozilla/5.0...
• Request URL: /api/v1/data
• Session ID: sess_abc123...
```

### 3. Пометка как выполненная
```
✅ Ошибка 5f8a9b2c отмечена как выполненная
Статус изменен: Open → Resolved
```

## Интеграция с проектом

Skill автоматически определяет:
- `BUGSNAG_DATA_API_KEY` - API ключ для доступа к данным
- `BUGSNAG_PROJECT_ID` - ID вашего проекта в Bugsnag

Если переменные окружения не установлены, skill подскажет как их настроить.

## Безопасность

- API ключи хранятся только в переменных окружения
- Все запросы выполняются через HTTPS
- Минимальные необходимые права доступа к API
- Логирование чувствительных данных отключено
