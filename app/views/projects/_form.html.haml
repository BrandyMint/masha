= simple_form_for @project do |f|
  = f.error_notification
  .form-inputs
    = f.input :name, :label => false, :require => true
    = f.input :slug, :label => false
  .form-actions
    = f.button :submit

