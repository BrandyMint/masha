-# Вход
-# @user

%h2 Доступные проекты

%table.table.table-condensed
  %tbody
    - @projects.each do |project|
      %tr
        %th= project
        %td
          %ul.horizontal-list
            - Membership::ROLES.each do |role|
              %li= change_role_link @user, project, role
