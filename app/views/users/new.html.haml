.page-header
  %h1 Регистрация
.row
  .span12
    = simple_form_for @user do |f|
      = f.error_notification
      .form-inputs
        = f.input :name
        = f.input :email
        = f.input :password
      .form-actions
        = f.button :submit, :class => 'btn btn-primary'

