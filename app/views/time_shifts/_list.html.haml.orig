- if time_shifts.present?
  %table.table.table-condensed.table-striped.table-hover.time-shifts-table
<<<<<<< HEAD
    %tr{:id => 'sticky-table-header'}
=======
    %thead
>>>>>>> master
      %th.col-lg-2
        = t 'time_shifts.date'
      %th.col-lg-1= t 'time_shifts.time'
      %th.col-lg-1= t 'time_shifts.project'
      %th.col-lg-2= t 'time_shifts.collaborator'
      %th.col-lg-5= t 'time_shifts.description'
      %th.print-hide.col-lg-1
    - time_shifts.each do |ts|      
      - ts = TimeShiftDecorator.decorate ts
      %tr{class: ts.tr_class}
        %td{ data: {'cell-title' => "#{t 'time_shifts.date'}" } }
          = ts.date
          -#%br
          -#%small.text-color-gray-light
            -#= ficon 'pencil-1'
            -#= l ts.updated_at

        %td{ data: {'cell-title' => "#{t 'time_shifts.date'}" } }
          %b= human_hours ts.hours
        %td{ data: {'cell-title' => "#{t 'time_shifts.project'}" } }
          = ts.project
        %td{ data: {'cell-title' => "#{t 'time_shifts.collaborator'}" } }
          = ts.user
        %td{ data: {'cell-title' => "#{t 'time_shifts.description'}" } }
          %small.muted= ts.description
        %td.print-hide
          = ts.update_link :css_class =>'btn btn-block', :label => t('.edit')

- else
  .alert.alert-warning Нет результатов
